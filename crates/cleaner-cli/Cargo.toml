[package]
name = "cleaner-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
cleaner-core = { path = "../cleaner-core" }
clap = { version = "~4.4", features = ["derive"] }
indicatif = "0.17"
colored = "2"
log = "0.4"
env_logger = "0.10"
